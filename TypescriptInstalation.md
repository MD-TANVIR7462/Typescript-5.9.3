1.First check node js version
2.install typescript by run the command globally - npm install -g typescript
3.check the typescript version by tsc -v
4.write typescript code and the on terminal type tsc index.js (eg) for transforming the code into javascript
5.now initial the configure type script file with the command - tsc --init this will configure a new tsconfig.json file 
  1.go to the file and 
  2.set outDir
  3.set rootDir
  4.then run tsc command it will automatically generate the javascript for the written code of typescript.
